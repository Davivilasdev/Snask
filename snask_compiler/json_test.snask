print("Teste módulo JSON");

// Devido a uma limitação atual do parser, strings JSON com aspas duplas escapadas não são suportadas diretamente como literais.
// Portanto, não podemos testar json_parse com JSON complexo que contenha strings.
// A alternativa seria construir o JSON programaticamente ou aguardar uma atualização do parser.

// Testando json_parse com JSONs simples que não exigem aspas duplas escapadas:
print("--- Teste json_parse com JSONs simples ---");
let empty_obj = json_parse("{}");
print("Empty Object Parsed: " + format("{0}", empty_obj)); // Esperado: {}

let empty_list = json_parse("[]");
print("Empty List Parsed: " + format("{0}", empty_list)); // Esperado: []

let number_val = json_parse("123.45");
print("Number Parsed: " + format("{0}", number_val)); // Esperado: 123.45

let bool_val = json_parse("true");
print("Boolean Parsed: " + format("{0}", bool_val)); // Esperado: true

let null_val = json_parse("null");
print("Null Parsed: " + format("{0}", null_val)); // Esperado: nil

let snask_list = [1, 2, 3];
print("Snask List: " + format("{0}", snask_list));
print("JSON List Stringified: " + format("{0}", json_stringify(snask_list)));